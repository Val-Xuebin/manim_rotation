# 2dr & 3dr — shared Python dependencies
# Optional: use conda for manim (e.g. conda activate manim) and pip for the rest.

numpy>=1.20
Pillow>=9.0
tqdm>=4.60

# Rendering (2dr, 3dr): Manim Community
# Install via pip or conda: conda install -c conda-forge manimce
manim>=0.18

# System dependency (not installed via pip): ffmpeg — required for video render and frame extraction.
